{
  "name": "live-static-preview",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A CodePen-style live HTML/CSS/JS preview tool built with vanilla JS and plain <textarea> editors. Runs with no build step and no runtime dependencies.",
  "keywords": [
    "playground",
    "live-preview",
    "sandbox",
    "iframe",
    "no-build",
    "vanilla-js"
  ],
  "license": "MIT",
  "scripts": {
    "start": "npx --yes serve .",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "comment": "NO RUNTIME DEPENDENCIES: `npm start` just serves the static files. Vite is optional — `npm install && npm run build` produces a minified, bundled production copy of the local ES modules.",
  "devDependencies": {
    "vite": "^5"
  }
}
